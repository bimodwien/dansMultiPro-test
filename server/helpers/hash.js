// 'use strict'
// const bcrypt = require('bcrypt') ;

// function makeHash(payload) {
//     return bcrypt.hashSync(payload, 10);
// };

// function compareHash(payload, hashed) {
//     console.log('ini payload >>>>',makeHash(payload));
//     console.log('ini hashed >>>>>',hashed);
//     return bcrypt.compareSync(makeHash(payload), hashed);
// };

// module.exports = { makeHash, compareHash }